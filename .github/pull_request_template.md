## What & why


## How verified

- [ ] `npm test` green locally
- [ ] `npx tsc --noEmit` (and `cd web && npx tsc --noEmit` for web changes)
- [ ] New routes/CLI commands added to the surface inventory + threat-matrix counts recomputed
- [ ] Web changes: `cd web && npm run build` still succeeds
