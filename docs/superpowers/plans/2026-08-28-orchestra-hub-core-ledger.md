# SDD ledger — plan: /Users/arminrad/Desktop/agentboard-hub-core/docs/superpowers/plans/2026-08-28-orchestra-hub-core.md

Spec: docs/superpowers/specs/2026-08-28-orchestra-hub-design.md (reachable, read)
Worktree: /Users/arminrad/Desktop/agentboard-hub-core on branch feat/hub-core (base caffda5)
Workspace: /Users/arminrad/Desktop/agentboard/.superpowers/sdd/2026-08-28-orchestra-hub-core

## Pre-flight conflict scan

### Cross-task pairs (shared files or interfaces)

| Pair | Produces → Consumes | Finding |
|------|--------------------|---------|
| T1 → T2 | `HubSql`, `withTransaction`, error family, `hubMigrate`, `HUB_MIGRATIONS` → T2 consumes all and appends 2 entries to `HUB_MIGRATIONS` | Clean. T2 explicitly re-states the full array. |
| T1 → T4 | `devices` table (001) → `mintDeviceToken`/`verifyDeviceToken` | Clean. Columns used match 001 exactly. |
| T2 → T3 | `appendOrgEvent`, `HubCard`, `cards` table (002) → card ops | Clean. `HubCard.column` ↔ column `column_name` mapped in `rowToCard`. |
| T2 → T5 | `mail`/`agents` tables (002), `appendOrgEvent` → mail + presence | Clean. `agents.state` CHECK matches `HubAgentState` union exactly. |
| T3,T4,T5 → T6 | all op functions + `verifyDeviceToken` → ops route + auth hook | Clean. Every op in the `OPS` set has a `runOp` case and vice versa. |
| T6 → T7 | `hubOpsPlugin`, `HubOpsRouteOptions`, `buildHubServer` → T7 modifies both to add `broadcast` | Clean. T7 gives explicit replacement text for each edit site. |
| T7 → T8 | `buildHubServer`, `hubMigrate` → `defaultStartHub` | Clean. |
| T6,T7 → T9 | 5 routes + 12 tables → inventory entries | **Conflict:** hub tables `agents`/`boards`/`cards` collide with existing SQLite table names already in `database_tables`. See Ruling R2. |

### Per-task self-consistency

| Task | Finding |
|------|---------|
| T1 | Clean. Test's inline PGlite adapter matches `HubSql`; files created match files tested. |
| T2 | Clean. Helper created in step 2 is imported by the step-1 test; run order (step 3) is after both. |
| T3 | Clean. `HubSql` args passed to `HubSqlPool` params are structurally assignable (`connect?` optional). |
| T4 | Clean. |
| T5 | **Conflict:** `sweepStalePresence` uses `($2 \|\| ' seconds')::interval` with a numeric bind param; Postgres may not infer the `text` operand. See Ruling R1. |
| T6 | Clean. `request.params` is populated before `onRequest` for matched routes. |
| T7 | Clean. `options.broadcast` is in scope where the plan edits the handler. |
| T8 | Clean. Commander `exitOverride` + throwing action ⇒ `parseAsync` rejects, as the test expects. |
| T9 | Depends on R2. |

### Rulings

- **R1 (T5 interval cast):** Implementer must use whichever cast form both PGlite and real Postgres accept — `make_interval(secs => $2::int)` preferred, `($2::text || ' seconds')::interval` as fallback — and prove it with the TTL test rather than transcribing the plan's form verbatim. Spec requires only "TTL lapse ⇒ offline"; the SQL spelling is not spec-binding. Cost if wrong: the presence TTL test fails on the same task, caught immediately.
- **R2 (T9 inventory table names):** If the docs test rejects duplicate table names, record hub tables namespaced as `hub:agents`, `hub:boards`, `hub:cards`, etc.; otherwise use plain names. Decided by running the docs gate, not by guessing. Cost if wrong: docs-test churn only, no runtime impact.
- **R3 (worktree):** All implementation happens in `/Users/arminrad/Desktop/agentboard-hub-core` on `feat/hub-core`, never the shared checkout — the user's global CLAUDE.md forbids branching there and other agents share it. Cost if wrong: work lands on main and disrupts concurrent agents.

## Progress

Task 1: dispatched (implementer hub-impl-1, sonnet, BASE caffda5)
Task 1: implementer DONE, commit e694fef. 2/2 new tests; suite 2563/2563 cases (9 files fail to load on missing web/node_modules react — implementer verified pre-existing on clean feat/hub-core). tsc clean, build verified.
Task 1: Ruling R4 — the brief's `hubMigrate` sent whole multi-statement .sql files through one query(); PGlite's extended-query protocol rejects that, so the implementer added a comment-stripping statement splitter. Accepted: the plan's one-call form simply does not work, and the splitter is sufficient for 001-003 (pure DDL, no $$-quoted bodies). Constraint carried into T2's dispatch: migrations 002/003 must stay pure DDL with no function bodies and no semicolons inside string literals. Cost if wrong: a future migration mis-splits and applies partially — mitigated by each migration running inside the runner's transaction.
Task 1: Ruling R5 — npm resolved pg ^8.23.0 / @types/pg ^8.23.1 instead of the brief's ^8.13.1 / ^8.11.10. Accepted: the brief's versions were floors I picked from memory, not spec-binding, and these are the same major. Cost if wrong: none foreseeable; a pg 8.x API change would surface in Task 8's pool wiring.
Task 1: review dispatched (hub-review-1, sonnet, caffda5..e694fef)
Task 1: review clean — spec MET on every requirement, task quality no issues. Reviewer independently verified tsc clean, no better-sqlite3 under src/hub/, src/server.ts untouched, 2/2 tests non-vacuous.
Task 1: minor (deferred): hubMigrate's statement splitter is naive — no $$-quoted body handling, no semicolon-inside-string-literal handling, and comment stripping is per-line indexOf('--') so a literal `--` inside a string would mis-strip. Safe for 001-003 (pure DDL, verified against the actual 001 file). Needs a SQL-aware splitter or a one-statement-per-call convention before any migration ships a function/trigger body. Surface to final review.
Task 1: complete (commits caffda5..e694fef, review clean)
Task 2: dispatched (implementer hub-impl-2, sonnet, BASE e694fef)
Task 2: implementer DONE, commit 9910e3f. 6/6 hub tests pass, tsc clean. Reported a correctness concern rather than silently shipping it.
Task 2: Ruling R6 — the brief's `appendOrgEvent` allocates seq with `(SELECT MAX(seq)+1 ...)` inside the INSERT. That is not race-safe: two concurrent appends in the same org read the same MAX and the loser hits UNIQUE(org_id, seq). The plan's own Done Criteria promise a gapless resumable stream for 3+ daemons, and Tasks 3/5/6/7 all append, so a racy primitive compounds. Decided: replace with an atomic per-org counter row (`org_event_seq`, upsert `ON CONFLICT DO UPDATE ... RETURNING`) added as NEW migration 004-hub-event-seq — never by editing the already-committed 003, which the append-only rule forbids. A single UPDATE...RETURNING takes a row lock, so it serialises correctly whether called bare or inside withTransaction, and works identically on PGlite and real Postgres. Cost if wrong: one extra table and migration; a flawed counter still fails loudly on the UNIQUE constraint rather than silently corrupting ordering.
Task 2: fix round 1 dispatched to hub-impl-2 (R6)
Task 2: fix round 1 (1 addressed, 0 open — seq now allocated from org_event_seq upsert, placed after the replay short-circuit so replays don't burn a number; migration 004 added, 003 untouched; commits 9910e3f..54a710e). 8/8 tests pass.
Task 2: review dispatched (hub-review-2, sonnet, e694fef..54a710e) — told the reviewer the brief's seq allocation is superseded by R6.
Task 2: review — spec MET on all interfaces; 0 Critical, 1 Important (appendOrgEvent's counter bump + insert are non-atomic when called bare, so a mid-failure burns a seq number), 2 Minor. Reviewer independently re-ran tests (8/8) and tsc. Confirmed the R6 counter fix genuinely closes the concurrent-writer race.
Task 2: Ruling R7 — took the reviewer's caller-contract option and explicitly REJECTED its alternative (making appendOrgEvent open its own transaction when handed a pool). Tasks 3 and 5 call it from inside an open withTransaction; in Postgres the inner COMMIT would commit the caller's outer transaction early, so a later failure in createCard could no longer roll back the event — that trades a benign gap for split-brain writes. Fix is therefore: restore the `tx` parameter name as the signal, state the transaction contract in the docstring, delete the line sanctioning bare calls, and record the accepted residual risk. Cost if wrong: a burned seq number on process death mid-append, which yields a gap but never a duplicate, a lost event, or a bad resume, since clients read `seq > since` ascending and a number with no row is simply never delivered.
Task 2: Ruling R8 — leaving `org_events.kind` without a CHECK constraint (reviewer Minor). It matches the brief, and Task 7 may add event kinds; a constraint here is one a later migration would have to fight. Cost if wrong: an invalid kind string could be written by a future bug and would surface at the consumer instead of the database.
Task 2: minor (deferred): none outstanding — dead `DEFAULT 0` folded into fix round 2; kind-CHECK parked under R8.
Task 2: fix round 2 dispatched to hub-impl-2 (R7 + dead default)
Task 2: fix round 2 (2 addressed, 0 open — tx rename + explicit contract docstring + residual-risk comment with never-reuse warning; dead DEFAULT 0 dropped; commits 54a710e..4fca6e3). Re-reviewer verified appendOrgEvent opens no transaction of its own, readOrgEventsSince/latestOrgSeq kept `sql`, rename source-compatible across ~17 call sites. No new breakage.
Task 2: complete (commits e694fef..4fca6e3, review clean)
Task 3: dispatched (implementer hub-impl-3, sonnet, BASE 4fca6e3)
Task 3: implementer DONE_WITH_CONCERNS, commit 507b9a6. 14/14 tests pass, tsc clean. Flagged that the idempotency-key check runs AFTER the entity mutation in all four card ops.
Task 3: Ruling R9 — the concern is load-bearing, fix now rather than defer. The brief placed the idempotency check inside appendOrgEvent, which runs after the mutation, so a replayed op mutates first and only then discovers the key. Two reasons this cannot wait: Plan 2's daemon offline queue replays queued ops after reconnect and MUST not double-apply (that is the entire purpose of the key), and Task 6's already-written test asserts a replayed op returns 200 twice and leaves exactly one card — unsatisfiable with the current ordering. Fix: new exported `findOrgEventByIdempotencyKey`, and each card op short-circuits on it inside the transaction before mutating, returning the entity from the stored event payload. The existing same-key/different-content ConflictError stays as a backstop. Also required: assert the event log does not grow on replay, since a second event would corrupt every daemon's resume. Cost if wrong: replay returns a stale entity snapshot from the stored payload rather than current state — acceptable, since the caller's next sync frame corrects it.
Task 3: Ruling R10 — I disputed the implementer's claim that replay "silently duplicates"; my read is the ConflictError inside the transaction rolls the duplicate back, yielding a spurious 409 instead. Rather than adjudicate on reading, required a failing test FIRST to establish observed behaviour empirically. Cost if wrong: none — the test settles it either way, and the fix is identical under both readings.
Task 3: fix round 1 dispatched to hub-impl-3 (R9 + R10)
Task 3: fix round 1 (1 addressed, 0 open — findOrgEventByIdempotencyKey added; all four ops short-circuit before mutating; commits 507b9a6..e1d6536). 17/17 tests pass.
Task 3: R10 resolved empirically — probe test showed NO duplicate row (the transaction rolls back), so the implementer's "silently duplicates" reading was wrong and mine was right. But the true defect was subtler than either: appendOrgEvent's content-equality check can NEVER match for createCard because every call mints a fresh id/created_at, so a retry always 409'd instead of returning the original success. Worth the test-first step — both of our code readings were incomplete.
Task 3: review dispatched (hub-review-3, sonnet, 4fca6e3..e1d6536) — told the reviewer the brief's idempotency placement is superseded by R9.
Task 3: review — spec PASS on all signatures; 0 Critical, 1 Important, 2 Minor. Reviewer independently re-ran 17/17 tests + tsc, and verified by code read (not report claim) that mutual exclusion is intact, stale writes touch 0 rows, 409s carry current state, every query filters org_id, replay leaves the event log at 1 row, and every appendOrgEvent call uses the mutation's own tx.
Task 3: Ruling R11 — the Important finding is a hole in MY directed fix (R9), not the implementer's work: short-circuiting on org_id+key alone bypasses appendOrgEvent's `prior.kind === input.kind` guard, so reusing one key across two different operations silently returns the first op's payload and mutates nothing. In a task whose purpose is "no silent duplicated work", silently returning another operation's result is the wrong failure mode. Fix: replayIfIdempotent takes the expected kind and throws ConflictError on mismatch. Cost if wrong: a legitimate replay could 409 if a kind were ever mislabelled at the call site — caught immediately by the replay tests.
Task 3: Ruling R12 — folded in reviewer Minor 2 rather than deferring. Two concurrent callers sharing a key can both pass the pre-check; the loser hit the raw unique index and surfaced a 500 to a replaying daemon. Decided the correct fix is semantic, not an error mapping: on unique violation, re-read the event for that (org_id, key) and RETURN it, because the winner already appended exactly the event this caller wanted, so the winner's event is the correct idempotent answer. Throw only if the re-read finds nothing or a different kind. Cost if wrong: if SQLSTATE 23505 cannot be detected reliably under PGlite the behaviour is unverifiable — instructed the implementer to report that rather than guess, since an untestable fix is worse than a documented gap.
Task 3: Ruling R13 — declined reviewer Minor 1 (non-keyed re-claim by the current owner bumps version and appends an event). It matches the brief, and the unbounded-growth scenario needs a caller that poll-reclaims as a heartbeat; Task 5's presence heartbeats are a separate mechanism that never touches cards, so no such caller exists in this design. Cost if wrong: if Plan 2's daemon ever reclaims on a timer, the event log grows per reclaim — revisit then.
Task 3: fix round 2 dispatched to hub-impl-3 (R11 + R12)
Task 3: fix round 2 (2 addressed, 0 open — replayIfIdempotent takes expectedKind and 409s on mismatch; appendOrgEvent catches the idempotency-index violation and returns the winner's event; commits e1d6536..195b01e). 20/20 tests pass.
Task 3: re-review verified the catch is scoped by constraint NAME (`org_events_idempotency_idx`) and so cannot swallow the bare `UNIQUE (org_id, seq)` constraint, which Postgres auto-names `org_events_org_id_seq_key` — a seq collision still rethrows. Re-reviewer wrote its own PGlite probe confirming the error carries `.constraint` and `.code='23505'` exactly as real Postgres does. Race helper judged honest: the 23505 error is genuine, only the two-writers-at-once timing is simulated (PGlite is single-connection).
Task 3: complete (commits 4fca6e3..195b01e, review clean)
Task 4: dispatched (implementer hub-impl-4, sonnet, BASE 195b01e)
Task 4: implementer DONE, commit fce22c5. 4/4 device tests, full hub suite 24/24, tsc clean. Concern raised: constantTimeEquals re-check after the SQL lookup is redundant (WHERE token_hash=$1 already guarantees exact match); kept per brief with a comment.
Task 4: review dispatched (hub-review-4, sonnet, 195b01e..fce22c5) — asked it to rule on whether the redundant constant-time check is harmless, misleading, or should go, and on last_seen_at being written on every verify.
Task 4: review APPROVE — spec met on every signature, 0 Critical, 0 Important, 2 Minor. Reviewer confirmed the plaintext-not-stored test inspects the real DB column, and that the cross-org revoke test asserts the victim token still verifies. Endorsed keeping the redundant constantTimeEquals WITH its comment over removing it: the comment documents that hash-then-indexed-lookup is the actual protection, which stops a future reader mistaking it for load-bearing timing defence or using it to justify loosening the query to a scan.
Task 4: Ruling R14 — accepted "keep + document" for the redundant constant-time check rather than deleting it. Both implementer and reviewer independently reached the same conclusion and the security argument (SHA-256 preimage resistance, not comparison timing, is what protects a hashed token) is correct. Cost if wrong: a few dead lines that a future reader might misread — mitigated by the comment that exists precisely to prevent that.
Task 4: minor (deferred): last_seen_at is UPDATEd on every authenticated verify, i.e. a write on every hub request once this is on the hot path. Not a defect and no test expects throttling; surface to final review as a scaling follow-up.
Task 4: carry-forward to Task 6 — error messages differ between unknown ("device token is not valid") and revoked ("device token has been revoked"). Both are 403 as required, but if the HTTP layer serialises `.message` into the response body the two cases become distinguishable, reintroducing the existence-leak that 403-for-both exists to prevent. Task 6 must confirm the client-facing body does not carry these strings.
Task 4: complete (commits 195b01e..fce22c5, review clean)
Task 5: dispatched (implementer hub-impl-5, sonnet, BASE fce22c5)
Task 5: implementer DONE_WITH_CONCERNS, commit bb0b67f. 12 new tests, full hub suite 36/36, tsc clean.
Task 5: R1 CONFIRMED — the brief's `($2 || ' seconds')::interval` is genuinely broken on PGlite, as the pre-flight scan suspected. Replaced with `make_interval(secs => $2::int)`, proven by TTL tests plus two the implementer added unprompted (within-TTL agent stays online; an agent that never heartbeat is still swept via NULL last_heartbeat_at).
Task 5: implementer found a defect neither the brief nor I anticipated — PGlite returns `created_at` as a native Date from a direct RETURNING but as a string when read back out of an event's JSON payload, so drainInbox's localeCompare sort crashed. Fixed via new Date(x).getTime(). Caught by the brief's own delivery test.
Task 5: authorised extracting replayIfIdempotent from cards.ts into events.ts as a generic rather than forking a second copy for mail. Flagged to the reviewer as the highest-risk part of the diff, since it edits already-reviewed mutual-exclusion code.
Task 5: review dispatched (hub-review-5, sonnet, fce22c5..bb0b67f) — also asked for a recommendation on same-kind/different-payload replay (open question, not a fix order).
Task 5: review — spec MET on all six signatures, 0 Critical, 0 Important, 1 Minor. Reviewer checked the cards.ts refactor call-site by call-site (all four kinds correct, extracted helper a byte-for-byte move) rather than trusting the green suite, and confirmed the Date-vs-string fix handles both shapes and that no other site in the diff has the same latent mismatch.
Task 5: Ruling R15 — accepted the reviewer's recommendation to keep kind-only replay checking and NOT add a request-fingerprint column. The failure it would catch (same key, same kind, different body) only arises from a client bug; the reconnect-replay case this mechanism exists for resends a byte-identical op, and Plan 2's daemon mints a fresh UUID per op, so no legitimate path reuses a key with different content. A fingerprint column means new schema, migration risk, and an extra write on every mutating op to catch something a daemon-side test finds faster. Cost if wrong: a buggy client reusing a key silently gets the original result and its second message is never sent — recoverable, and a cheap isolated follow-up if it ever appears.
Task 5: minor (deferred): registerAgent does SELECT-then-INSERT with no row lock, so two concurrent registrations of the same new agent name can both pass the existence check and race the UNIQUE(org_id, board_id, name) constraint, surfacing a raw DB error rather than returning the existing row. Pre-existing in the brief's design, untested either way. Surface to final review; relevant because Plan 2's daemon registers agents on reconnect.
Task 5: complete (commits fce22c5..bb0b67f, review clean)
Task 6: dispatched (implementer hub-impl-6, sonnet, BASE bb0b67f)
Task 6: implementer DONE_WITH_CONCERNS, commit 94fb811. 10/10 server tests (brief's 6 + 4 added), full hub suite 46/46 run 3x, tsc clean.
Task 6: the brief's own reference server.ts contained the exact token-existence oracle the dispatch warned about — its catch block sent verifyDeviceToken's raw .message to the client. Implementer replaced it with a single hardcoded generic body for all auth failures and tested the two responses are byte-identical. That is the fourth real defect found in my plan text.
Task 6: INCIDENT — implementer ran `git stash`/`git stash pop` in the worktree to test a hypothesis. Stash is a single per-repository namespace shared across ALL worktrees, so the pop applied an unrelated pre-existing stash ("wiki-182 pre-merge cycle") belonging to another agent's work, conflicting 9 unrelated tracked files. It caught this immediately and restored those paths. I verified independently rather than trusting the report: `git stash list` shows all 9 entries intact (stash@{0}..stash@{8}), both working trees clean, and the hub branch is exactly the 10 expected commits. No data lost. Root cause worth carrying: git stash is shared repo state, so it is never safe in a multi-agent checkout even from an isolated worktree.
Task 6: review APPROVE — spec MET on every requirement, 0 Critical, 0 Important, 2 Minor. Reviewer verified the org-check mechanism against installed Fastify 5.10.0's actual lifecycle (routing populates params before onRequest) rather than trusting my brief's claim or the implementer's; enumerated all 5 routes confirming each derives org solely from request.hubOrgId with no body/query path; and confirmed via a forced raw Postgres CHECK violation (SQLSTATE 23514) that no constraint name, sqlstate, or stack frame reaches a client.
Task 6: minor (deferred): three distinct 403 bodies exist (no token / invalid-or-revoked / wrong-org). Only the invalid-or-revoked case had to be self-indistinguishable, and it is; the others create no token-existence oracle. Also: an unconditional `cache-control: no-store` on every hub request is beyond the brief — harmless hardening. Surface both to final review.
Task 6: minor (deferred): the task-6 report's "Ambiguities decided" section inaccurately claims `...common` (with idempotencyKey) is spread into agent.register/agent.heartbeat; neither the brief nor the code does that. Report-only inaccuracy, no code defect — noting so the final reviewer does not trust that section.
Task 6: complete (commits bb0b67f..94fb811, review clean)
Task 7: dispatched (implementer hub-impl-7, sonnet, BASE 94fb811)
Task 7: implementer DONE, commit fad73f9. 57 tests across 8 hub files (11 new in hub-sync), tsc clean, no blocking concerns.
Task 7: BOTH pre-flagged traps were real, vindicating flagging them rather than letting the brief be transcribed. (a) Fastify 5 does require `reply.hijack()` — the implementer traced it to `wrap-thenable.js` and found the brief's return-reply pattern only works for handlers that never await, while this route must await readOrgEventsSince. (b) The backlog↔live boundary was NOT protected by the brief's cursor check; it was fixed and covered by two deterministic tests with an injectable pageSize forcing a multi-page drain. That is now the fifth and sixth real defect found in my plan text.
Task 7: review dispatched (hub-review-7, sonnet, 94fb811..fad73f9) — asked it to trace the boundary sequence itself and judge whether the tests genuinely force the interleaving or merely appear to.
Task 7: review — spec MET, 0 Critical, 2 Important, 1 Minor. Reviewer hand-traced the boundary (subscribe-before-drain, buffer while draining, sort-and-flush against the final cursor) and confirmed the fix is correct AND that the test genuinely forces the interleaving by holding page 2's promise open — not a test that merely looks like a race. Also independently confirmed both brief defects the implementer reported.
Task 7: Ruling R17 — fix the mid-drain disconnect leak now, not later. The close handler is registered only after the drain resolves while the subscription attaches before it, so a disconnect during a long backlog drain fires the socket's one-shot 'close' with no listener and leaks both the subscription and the subsequently-installed ping timer. That is precisely the flaky-network reconnecting-daemon case the feature exists for, so it will occur in production. Fix: register cleanup before the drain, make it idempotent, and bail if `request.raw.destroyed` after the drain. Cost if wrong: cleanup running twice, which idempotency covers.
Task 7: Ruling R18 — fix the over-publish race inside HubBroadcaster.publish (per-org highest-published-seq guard), explicitly NOT by threading appended-event lists back through runOp and the seven op functions. The guard is atomic because publish is synchronous on a single-threaded runtime, and it fixes the contract regardless of what callers pass; the alternative churns signatures across already-reviewed safety-critical card code for no additional correctness. Today SSE clients are unharmed only because the per-connection cursor absorbs the duplicate, but Plan 3 adds web clients on this same broadcaster, so leaving the contract wrong is borrowing trouble. Cost if wrong: a per-org map entry per org (bounded by org count, documented in a comment so it is not later mistaken for a leak).
Task 7: fix round 1 dispatched to hub-impl-7 (R17 + R18)
Task 7: fix round 1 (2 addressed, 0 open — teardown registered before the drain with an idempotent `torndown` gate and a post-drain destroyed check; per-org highest-seq guard in HubBroadcaster.publish; commits fad73f9..eb036cd). 60/60 tests.
Task 7: re-reviewer verified idempotency by reading both teardown paths (ping is structurally unreachable once torndown is set), confirmed the mid-drain test holds a second page read genuinely in flight at abort time, and confirmed the guard is per-org via a two-org test (org B's seq 1 still delivered after org A's seq 5). Re-reviewer went idle without reporting and had to be chased — second occurrence; do not close a task on an assumed verdict.
Task 7: minor (deferred): if two org_events commits ever become visible out of seq order, the publish guard could permanently suppress the LIVE delivery of the lower seq. Not data loss — the client recovers on reconnect via ?since=, which is the documented contract of this system — but worth noting. Surface to final review.
Task 7: complete (commits 94fb811..eb036cd, review clean)
Task 8: dispatched (implementer hub-impl-8, sonnet, BASE eb036cd)
Task 8: implementer DONE_WITH_CONCERNS, commit 67dc5a1. 4 new tests; full suite 2624/2625 with 10 pre-existing web/react failures (web/node_modules not installed in this worktree) + the known beta-quality-matrix failure. Implementer verified both predate the task by reverting and re-running against clean eb036cd.
Task 8: seventh real defect found in my plan text — the brief's pg.ts let a HubSqlConnection keep querying after release(), i.e. issuing statements on a pooled client pg may have already handed to another caller. Implementer added a release guard.
Task 8: review CLEAN — spec met on every point, 0 Critical, 0 Important, 0 Minor. Reviewer executed `import('pg')` + `new pg.Pool()` live in the worktree (resolves via pg/esm/index.mjs) rather than accepting a type-check as proof, read pg-pool's source to confirm double-release already throws upstream so the new guard closes only the real gap (post-release query), verified bare `orchestra` short-circuits at cli.ts:969 before parseAsync so the local daemon path is untouched, and grepped every databaseUrl use to confirm credentials are never logged or thrown.
Task 8: complete (commits eb036cd..67dc5a1, review clean)
Task 9: dispatched (implementer hub-impl-9, sonnet, BASE 67dc5a1) — scope EXTENDED per R16 to include docs/quality/beta-quality-requirements.json.
Task 9: implementer done, commit 4f90df0. Required gates green (49 tests), full repo suite 2627/2627, only the environmental react-load failures remain. Reports 8 defects found in the brief.
Task 9: OPEN QUESTION 1 — implementer REVERTED the plan-required threat-matrix entries (TB-012/CUR-019/REM-T23/AC-21) because test/remote-ops-adversarial-contract.test.ts hardcodes abuse_cases to a closed executable-contract set (AC-01..AC-20) covering the LOCAL daemon's phone-pairing model. It documented the cross-org boundary as prose in the surface inventory instead, with hub-cross-org-isolation.test.ts as evidence. Asked the reviewer to verify the collision is real and to recommend accept / extend the adversarial contract / separate hub threat file. Not ruling until that lands — the hub's central new risk being the only informally-recorded one is exactly the gap the spec §9 warned about.
Task 9: review APPROVE — 0 Critical, 0 Important, 2 Minor (both defects in my brief, not the implementer's work).
Task 9: Ruling R19 (resolves OPEN QUESTION 1) — ACCEPT the threat-matrix revert. Reviewer independently confirmed the collision is real by reading test/remote-ops-adversarial-contract.test.ts:41-45: it asserts exact equality against AC-01..AC-20, so adding AC-21 fails outright, and the set is genuinely executable-contract-bound (REMOTE_SECURITY_ACCEPTANCE_IDS drives cases that actually run against a pairing-model target). Prose in the surface inventory with hub-cross-org-isolation.test.ts as executable evidence is an adequate stopgap; a separate hub matrix file is not justified by one boundary. FOLLOW-UP REQUIRED (not this plan): a hub-specific adversarial harness against the hub's Fastify inject(), a few hours of real work. Cost if wrong: the hub's cross-org risk stays informally recorded while local-daemon risks are machine-checked — mitigated by the isolation test being real and cited exactly.
Task 9: Ruling R20 (resolves OPEN QUESTION 2) — ACCEPT the pin update. Reviewer found 8+ prior commits, including ones by the repo owner, with exactly this shape (classify a file, recompute the discovery digest, recompute the pin, no new evasion tests), so "new evasion tests" applies to schema/contract changes rather than appending a filename to an existing list. The gate is not weakened for future changes: the pin still requires exact match, and any later JSON edit without updating it still fails. Cost if wrong: a future contributor cites this as precedent for a pin bump that DOES warrant evasion tests — mitigated by the pin forcing review every time.
Task 9: minor (deferred): my brief's recompute snippet used `d.route_sources.http_routes` but the live JSON is top-level `d.http_routes`; and the brief's table list omitted `org_event_seq` (12 vs the correct 13). The implementer worked around both — totals verified by independent recomputation (routes 274, tables 202, cli 161).
Task 9: complete (commits 67dc5a1..4f90df0, review clean)
ALL 9 TASKS COMPLETE. Final whole-branch review dispatched (hub-final-review, OPUS, caffda5..4f90df0, 14 commits) with the full deferred-minor and parked list for triage.
FINAL REVIEW: NOT READY TO MERGE — 1 Critical, 4 Important (2 blocking), 5 Minor. Reviewer had to be chased for its report (third occurrence). It executed rather than reasoned: real tsup build, live probes, PGlite rollback demonstration; left the worktree clean.
FINAL C1 (CRITICAL, blocks) — `orchestra hub` CANNOT START. hub-cli.ts dynamically imports ./hub/pg.js and ./hub/migrations.js; tsup's single-entry ESM code-splitting flattens those to chunks at the dist ROOT, so migrations.ts's import.meta.url resolves to dist/migrations/ while copy-hub-migrations.mjs writes dist/hub/migrations/. Reproduced as a real ENOENT at boot. Nine scoped reviews missed it because every test imports from src/ and hub-cli.test.ts injects a stub startHub, leaving the entire production boot path uncovered. This is the single most valuable finding of the whole run.
FINAL I1 (IMPORTANT, blocks) — cross-tenant id references accepted. cards.ts checks a foreign board_id (404) but mail.ts inserts card_id/reply_to unchecked and presence.ts inserts current_card_id unchecked. Live probe: foreign card_id → 200, nonexistent → 500, i.e. an existence oracle across every tenant. Survived because hub-cross-org-isolation.test.ts only varies the TOKEN, never a foreign id under a valid token. This is exactly what a hub adversarial harness (R19 follow-up) would have caught.
FINAL I2 (IMPORTANT, blocks) — hubMigrate has no transaction and no lock: a mid-migration failure half-applies the schema with nothing recorded, and concurrent hub boots race the PK insert. Bundled the $$-splitter weakness here, since the migration needing a transaction is the same one needing a function body.
FINAL: corrects an earlier ruling — the Task 7 deferred minor (publish guard suppressing a lower seq) is RETIRED as unreachable. appendOrgEvent's ON CONFLICT DO UPDATE RETURNING holds a row lock on the org counter until commit, so per-org commit order IS seq order and the required inversion cannot occur. Reviewer also warns this invariant is undocumented and that events.ts's docstring blessing bare calls would break it — fix noted for Plan 2.
FINAL: raises registerAgent's race in priority (before Plan 2, not before merge) — Plan 2's daemon registers on reconnect, so every hub restart reconnects all daemons at once and turns a rare race routine.
FINAL: core promise VERIFIED — no double-claim, no missed event, no divergence, with the row-lock reasoning spelled out. Blockers are a build bug and a scoping hole, not a design failure.
FIX WAVE COMPLETE, commit 64fc564. Built CLI booted against REAL Postgres 16, applied all 4 migrations, served; four simultaneous boots serialised on the advisory lock. Full suite 2703 tests / 335 files green.
FINAL RE-REVIEW (OPUS): all 6 ADDRESSED, no new Critical/Important breakage, MERGE-READY. Re-reviewer verified rather than assumed: it re-pointed the copy script to the broken path and confirmed the new test fails with the exact production ENOENT thrown from a built-chunk stack frame (proving it loads dist, not src); it independently enumerated all 6 client-reachable ops and matched the fixer's scope audit; it reverted the three requireOrgEntity calls and confirmed exactly 5 tests fail, proving they are load-bearing; and it confirmed the migration SQL is byte-unchanged.
FINAL RE-REVIEW corrections to the fixer's claims, worth keeping: (a) the U&'...' UESCAPE case is NOT a gap — U& changes the unicode escape char, not quote termination, so the scanner already parses it correctly; (b) the heartbeat transaction is justified but NOT uniquely necessary (a single-statement CTE would also be atomic) — the real cost is the highest-frequency op now holding a checked-out connection across 3-4 round trips; (c) crucially, heartbeat still does not call appendOrgEvent, so it never contends on the per-org counter — if it did, every heartbeat would serialise the whole org.
FINAL RE-REVIEW on the advisory lock: adequately evidenced, NOT unproven — but explicitly not by the four-way boot, which cannot discriminate (the pre-fix collision was never reproduced, so "no crash" is equally consistent with a working lock and a race that did not fire). What carries the weight is that the project's own code is tested (lock first, unlock last including on the throw path, all on one pooled connection, with a regression to pool-level queries failing the test) while the uncovered part is Postgres's documented contract, not a project assumption.
Ruling R21 — PARKED, not fixed: migrations.ts swallows a failed pg_advisory_unlock. On a healthy connection it returns false rather than throwing, and a broken connection releases the lock by dying; but if it ever did fail, the connection returns to the pool still holding a session lock and a later boot would block forever with no signal. Parked because the process allows exactly one fix wave and this is not load-bearing for merge. Cost if wrong: a silent indefinite hang on a future boot — cheap to pre-empt by logging, so it heads the follow-up list.
Ruling R22 — PARKED: test/hub-server.test.ts's error-handler probe depends on `expected_version` not being int4-bounded, the same shape of fragility as the heartbeat bug just fixed. Tighten that validation later and the test silently stops testing anything. Cost if wrong: a probe that passes while covering nothing — no product impact.
Ruling R23 — the fix report misstates hub test counts (66→83, not "83→106"; the 83-passing figure is right). Corrected here rather than in a second fix wave. Cost if wrong: none, a documentation number only.
NO SECOND FIX WAVE — residual minors surface to the operator at handover, per process.
Fix wave dispatched (hub-fix-final, OPUS, one dispatch): C1, I1, I2, I3, G3 comment, M5 rename. Explicitly NOT fixing: drainInbox destructive GET, registerAgent race, ops seq semantics, 500-event cap, recipientless sendMail, counter throughput ceiling.
Task 9: OPEN QUESTION 2 — implementer edited PINNED_REQUIREMENTS_SHA256 in scripts/check-beta-quality-matrix.mjs, whose own comment says pins are "intentionally independent of the JSON files. Changing a contract requires a reviewed code change... not a self-authorized JSON edit." I verified independently: the new pin exactly equals the recomputed SHA-256 of the requirements JSON (not fabricated), and the JSON diff is exactly the two intended fields — src/hub/types.ts added in correct alphabetical position, discovery digest updated; nothing removed or weakened. Preliminary read: the pin's purpose is to force a reviewed change and that review is happening now, so this is the intended workflow rather than a bypass. Asked the reviewer whether "and new evasion tests" imposes an unmet obligation and whether the gate is weakened for FUTURE changes.
Task 8: review dispatched (hub-review-8, sonnet, eb036cd..67dc5a1) — asked specifically whether `pg`'s CommonJS default import genuinely resolves at RUNTIME under NodeNext (type-check passing is not proof; failure would first appear in production), and whether bare `orchestra` still starts the local daemon rather than the hub.
Task 6: Ruling R16 — `test/beta-quality-matrix.test.ts` now fails, and it is OUR doing, not pre-existing: `src/hub/types.ts` declares `HubAgentState`, which matches the release gate's state-machine discovery pattern, so the file must be listed in `classified_state_machine_files` and the pinned `state_machine_discovery_sha256` recomputed in `docs/quality/beta-quality-requirements.json`. My plan never accounted for this gate — Task 9 covers the surface inventory and threat matrix but not the beta-quality requirements. Decided: extend Task 9's scope to include it rather than open a fix round on Task 6, since it is registration metadata of exactly the kind Task 9 already owns, and the gate blocks release-preflight, not the hub tests. Cost if wrong: the branch cannot pass `release:preflight` until Task 9 lands — acceptable, as nothing ships before then.
Task 3: carry-forward — sendMail (Task 5) needs the same idempotent-replay short-circuit; include in that dispatch.
