# SDD ledger — plan: /Users/arminrad/Desktop/agentboard-hub-cloud/docs/superpowers/plans/2026-08-28-orchestra-hub-cloud.md

Spec: docs/superpowers/specs/2026-08-28-orchestra-hub-design.md (read)
Worktree: /Users/arminrad/Desktop/agentboard-hub-cloud on branch feat/hub-cloud, based on feat/hub-core (base 5b91da1)
Workspace: /Users/arminrad/Desktop/agentboard/.superpowers/sdd/2026-08-28-orchestra-hub-cloud
Prior plan: Plan 1 complete on feat/hub-core, 16 commits, final review merge-ready.

## Pre-flight conflict scan

### Cross-task pairs (shared files or interfaces)

| Pair | Produces → Consumes | Finding |
|------|--------------------|---------|
| T1 → T2,T3,T5,T6 | `hubEnv()`/`HubEnv` → every later task reads config from it | Clean. T1 deliberately makes Clerk/Stripe fields optional so it can ship and deploy before billing exists. |
| T1 → T8 | `dist/hub-entry.js`, Dockerfile, `start` script → Railway deploy | Clean. |
| T2 → T7 | `hubApi.ts`, `hubConfigured()` → HubBoard/BillingPage | Clean. |
| T3 → T4 | Clerk principal + `orgs.clerk_org_id` mirror lookup → webhook writes those mirror rows | **Ordering risk:** T3 resolves orgs through mirror rows that only T4 populates. T3's tests must seed mirror rows directly rather than depending on T4. Called out in T3's dispatch. |
| T4 → T6 | membership rows → seat-cap enforcement | Clean; T4 step 4 defers the cap decision to T6's entitlements. |
| T5 → T6 | cached quantity columns (migration 005) → `entitlementsFor` | Clean. |
| T3,T4,T5 → `src/hub/server.ts` | all three mount routes/hooks on the same file | **Serial-only:** three tasks modify one file. Never run these implementers in parallel (already the rule). |

### Per-task self-consistency

| Task | Finding |
|------|---------|
| T1 | Clean. Build-assertion test is the load-bearing part. |
| T2 | Clean. Note `@fastify/cors` is a NEW dependency — the hub has no CORS today. |
| T3 | Depends on mirror rows; see pair finding above. |
| T4 | Clean. Raw-body requirement is explicit. |
| T5 | Clean. Lookup-key-not-price-id is stated as a test assertion, not just prose. |
| T6 | Clean. |
| T7 | **Cross-branch dependency:** the Clerk sign-in UI lives on `feat/clerk-web` (8ff6f15), NOT on this branch or main. T7 must merge/cherry-pick it. Called out in the plan and must be repeated in the dispatch. |
| T8 | Blocked on operator credentials (Supabase/Railway/Vercel/Clerk secret/Stripe restricted key). Tasks 1-7 need none. |

### Rulings

- **RC1** — Tasks 1-7 require no operator credentials; only T8 does. Confirmed to the operator so they can provision in parallel. Cost if wrong: a task stalls waiting on a secret — mitigated because every test uses PGlite and injected env.
- **RC2** — Plan 3 branches from `feat/hub-core`, not `main`, since it builds directly on Plan 1's hub. `feat/hub-core` is not yet merged; if the operator merges it to main later, this branch rebases cleanly or merges after it. Cost if wrong: a later merge conflict confined to `src/hub/`.
- **RC3** — Stripe catalogue already exists in test AND live (per the pricing memo). Plan 3 writes integration only and must never create or mutate products/prices. Cost if wrong: duplicate products in a live account taking real money — hence stated as a constraint, not a preference.

## Progress

Task 1: dispatched (implementer cloud-impl-1, sonnet, BASE 5b91da1)
Task 1: implementer complete, commit 84bfc5b. hub tests 94/94, full repo 2655/2655 (9 web suites fail to load on missing web/node_modules — pre-existing/environmental). Verified with a REAL build and a REAL boot against a disposable postgres:16-alpine container: migrations applied, /healthz 200, unauthenticated hub route 403. Also checked the failure modes (missing HUB_DATABASE_URL, unreachable DB) produce clean errors rather than module-resolution or ENOENT noise.
Task 1: two self-found defects beyond the brief — (1) static imports in hub-entry.ts made tsup split hub/migrations.ts into a facade + shared chunk, breaking test/hub-build-migrations.test.ts's "exactly one chunk exports hubMigrate" assumption, so it mirrored defaultStartHub's dynamic imports instead; (2) a top-level main() call crashed that same test's file scanner on import, so main() is now guarded by an entry-module check. Both are consequences of the bundler behaviour that caused Plan 1's boot bug, so this area stays fragile — the build-assertion test is what keeps it honest.
Task 1: review — spec MET on every point, but 1 CRITICAL: the build-assertion test cannot fail. It scans only dist/hub-entry.js's own text and never the chunks it imports. Reviewer PROVED it by adding `import './daemon.js'`, rebuilding (tsup emitted a 3.96MB shared chunk with 6 better-sqlite3 + 1 node-pty references, statically imported by hub-entry.js) and observing the test still pass 3/3. Reverted cleanly. The shipped bundle is genuinely clean — verified across all 5 reachable chunks — so this is a safety-net hole, not a live defect.
Task 1: Ruling RC4 — fix the test now rather than deferring. The native-dependency split IS the reason Task 1 exists, and its failure mode is invisible: a future import keeps the test green and first surfaces as a Railway build needing a C++ toolchain for code the hub never calls. Required the fix to be PROVEN by observing the test fail on a reintroduced regression and pass again after revert, plus a report of which chunk filenames the walk scanned (reviewer counted 5 reachable; a walk finding 1 is broken the same way). Cost if wrong: a walk that terminates early looks identical to a passing test — hence demanding the observed failure.
Task 1: review also confirmed — no secret values in thrown messages, pino default serializers so no header/body logging (bearer tokens safe), trustProxy weakens nothing (nothing in src/hub/ reads client IP or protocol for a security decision), src/cli.ts untouched so bare `orchestra` is unaffected, and --port correctly takes precedence over $PORT.
Task 1: fix round 1 dispatched to cloud-impl-1 (RC4)
Task 1: fix round 1 complete, commit 98c37cb. Test now walks the emitted module graph; 14/14 across 4 suites; clean-build walk reaches the same 5 chunks the reviewer counted. Both demanded proofs delivered: the reintroduced `import './daemon.js'` regression made the test FAIL 2/3, and reverting restored 3/3.
Task 1: RC4 VINDICATED — the implementer's FIRST version of the walk had a regex bug that missed esbuild's bare side-effect import form (`import "./chunk.js";`, no `from`), which is exactly how the offending chunk is referenced. That version stopped at 4 files and STILL PASSED against the regression. It was caught only because the ruling required observing the failure rather than asserting the fix. This is the strongest evidence in either plan for demanding demonstrated failure modes on any test whose job is to catch a silent regression.
Task 1: re-review ADDRESSED, no new breakage. Re-reviewer tested all five import forms against the regex (bare side-effect with and without semicolon, namespace, `export *`, `export {} from`, dynamic) — all matched; the only miss is a bare import not at line start, which esbuild's non-minified output never emits (it checked the actual chunks). It ran the regression itself: 2/3 failed on the offending chunk, then 3/3 after revert, worktree left clean. Cycle protection is a real visited Map checked before processing. Walk reaches the same 5 chunks. Original assertions retained, not replaced.
Task 1: complete (commits 5b91da1..98c37cb, review clean)
Task 2: dispatched (implementer cloud-impl-2, sonnet, BASE 98c37cb)
Task 2: implementer done, commit 9884a80. Root suite 338 files / 2720 passed; hub-cors 6/6; new standalone web suite 10/10; tsc clean both sides; real web build clean.
Task 2: three defects in my brief — (1) I claimed @clerk/react was already a web/ dependency; it is not on this branch (it lives on the unmerged feat/clerk-web), so it was added; (2) my file list omitted src/hub-entry.ts, but without threading env.webOrigin into buildHubServer() there, CORS would never activate in the deployed process while every test still passed — the exact shape of Plan 1's boot bug, caught this time before shipping; (3) I asked for a web/vite.config.ts change that is unnecessary, since Vite's default envPrefix already covers VITE_*; the implementer verified with a real build rather than just complying.
Task 2: Ruling RC5 — web/ now has its own vitest config and suite (`cd web && npm test`), NOT wired into the root `npx vitest run`. The implementer proved a root-level test cannot mock @clerk/react for hubApi.ts: module resolution walks up from the importing file and web/node_modules is not an ancestor of root test/, so vi.mock silently fails to intercept. Accepting the split suite, but CI must run it or it protects nothing. Decided to wire `npm test --prefix web` into ci.yml as part of Task 8 (the deploy/CI task) rather than reopening Task 2. Cost if wrong: the web suite silently never runs in CI — mitigated by making it an explicit Task 8 step, not a note.
Task 2: carry-forward — hubFetch prepends `/api/v1/hub` to paths. That matches where the hub mounts its routes, so it is correct, but it is a convention no later brief documents. Confirm it in Task 7's dispatch when the first real UI call is wired.
Task 2: review — spec MET on all three interfaces, 0 Critical, 1 Important (the CI gap already tracked under RC5), 3 Minor. Reviewer read @fastify/cors's own source to confirm the origin callback returns a boolean so the header can never be `*`; verified exact-string origin matching with a `.evil.tld` case (no substring bypass); confirmed null/absent/"null" origins get no headers and still hit the auth hook; and confirmed CORS registers BEFORE the device-auth hook. It also independently confirmed the production wiring through hub-entry.ts, i.e. that my omitted file really would have disabled CORS only in production.
Task 2: reviewer refined RC5's severity usefully — the security-critical suite (test/hub-cors.test.ts) IS under root test/ and so DOES run in CI; only the client-side plumbing (token forwarding, unconfigured-mode guard) is unprotected. So the CI gap is Important, not Critical, and Task 8 wiring remains the right call.
Task 2: minor (deferred): `Vary: Origin` is also emitted on rejected-origin preflights (correct behaviour; a code comment slightly overstates "no CORS headers"). VITE_HUB_BASE_URL has no runtime shape validation — build-time-baked deploy config, same trust tier as WEB_ORIGIN, not attacker-influenced, so not a credential-leak vector. `__setHubEnvOverrideForTest` is a test seam in a shipped module — justified, since import.meta.env is baked at transform time even under vitest (reviewer verified this is a real constraint, not an excuse).
Task 2: expected-state note for Task 3 — the hub's auth hook currently treats EVERY `Bearer <token>` as a device token, so a real Clerk JWT from hubFetch 403s today. That is exactly what Task 3 fixes; nobody should be surprised end-to-end auth is not yet live.
Task 2: complete (commits 98c37cb..9884a80, review clean)
Task 3: dispatched (implementer cloud-impl-3, sonnet, BASE 9884a80)
Task 3: implementer done, commit 942f5d4. 11 new tests, hub suite 94/94, full repo 2731/2731, tsc + build clean.
Task 3: library gotcha worth keeping — @clerk/backend's package-root `verifyToken` is a THROWING wrapper returning Promise<JwtPayload>, not the {data, errors} shape its own docstring example implies. Cost the implementer a wasted round. Getting this backwards in future Clerk work could mean treating a failed verification as success, so it is flagged to the reviewer as a correctness check rather than trivia.
Task 3: OPEN QUESTION — clerkSecretKey is threaded only through hub-entry.ts (Railway), leaving `orchestra hub` (local) device-only, the same split Task 2 made for webOrigin. Asked the reviewer whether that is a sensible hosted/local separation or a footgun where someone testing hosted mode locally silently gets device-only auth with no signal. Not ruling until that lands.
Task 3: review — spec MET, 0 Critical, 0 Important, 1 Minor. Reviewer verified against the COMPILED @clerk/backend v3.16.12 in node_modules (not the .d.ts) that the package-root verifyToken is `withLegacyReturn(verifyToken)` which throws on failure and resolves the payload on success — exactly what clerk.ts's try/catch assumes. Getting that backwards would have been a treat-failure-as-success bug; it is not backwards. Also traced the revocation JOIN and ran the removed-member test: the org_id claim is only ever a lookup key, so deleting a membership 403s the very next request with an unchanged token. Confirmed both deviations (returning userId, narrowing the env param) are justified.
Task 3: reviewer's judgement on the oracle — the "not a member of this org" 403 is deliberately a distinct body, and that is correct rather than a leak: it only fires for a cryptographically valid, currently-live session, which is the same information class the device path already exposes with its own distinct message. No new distinguishable failure class exists for UNAUTHENTICATED callers.
Task 3: Ruling RC6 (resolves the open question) — thread webOrigin AND clerkSecretKey through hub-cli.ts's defaultStartHub. The reviewer's read is right: this is a footgun, not a hosted/local separation. Running `orchestra hub` locally against a real Clerk app yields silent device-only auth — Clerk tokens 403 with the deliberately uninformative generic body, indistinguishable from a misconfigured Clerk application. The very design that protects against attackers makes this misconfiguration undiagnosable, so the fix also adds a startup line stating whether Clerk auth is enabled (on/off only, never the key or any prefix of it). Also closes the same gap Task 2 left for webOrigin. Cost if wrong: a configured local run starts honouring Clerk where it previously ignored it — which is the intent; unconfigured runs are unchanged because buildHubServer already no-ops on absent options.
Task 3: fix round 1 dispatched to cloud-impl-3 (RC6)
Task 3: fix round 1 (1 addressed, 0 open — webOrigin + clerkSecretKey threaded through defaultStartHub from hubEnv(); on/off Clerk log line; commits 942f5d4..07b3884). Full repo 2732/2732; build-assertion tests re-verified against the new chunk layout.
Task 3: re-review ADDRESSED. Confirmed an unconfigured local run is byte-identical to before (both options are truthiness-gated, so {undefined, undefined} behaves as {}), the new test exercises the REAL defaultStartHub with no startHub override so reverting the threading would fail it, the log line prints only `clerk auth: enabled|disabled` with no key/prefix/length, and clerk.ts + server.ts's auth hook + hub-entry.ts are untouched.
Task 3: Ruling RC7 — PARKED, not fixed: `hubEnv()` is called with no argument inside defaultStartHub, so it always reads real process.env and never the injected `deps.env`. Result is a split where port and databaseUrl honour injected env while webOrigin and clerkSecretKey do not. Parked because production impact is nil (real callers never inject deps.env, so process.env is correct either way) and the reviewer explicitly judged it not fix-now; spending a round would churn code that just passed security review. Cost if wrong: a future test author writes an end-to-end deps.env test that silently diverges or throws depending on the runner's real environment — cheap one-line fix (`hubEnv(deps.env)`) whenever someone needs it. Surface at final review.
Task 3: complete (commits 9884a80..07b3884, review clean)
Task 4: dispatched (implementer cloud-impl-4, sonnet, BASE 07b3884)
Task 4: implementer done, commit 9c1c677. 13 new webhook tests; full repo 340 files / 2745 tests; tsc clean.
Task 4: three disclosed deviations — (a) no Svix message-id ledger, idempotency is structural via upserts, because the brief ruled out a new migration; (b) membership.created/updated throws 500 when the org or user is not yet mirrored so Clerk retries, handling out-of-order delivery at the cost of a permanently-missing dependency retrying forever with no distinct signal; (c) threaded clerkWebhookSigningSecret through hub-cli.ts and hub-entry.ts (outside the brief's file list) since the route would otherwise be dead code outside tests — the same production-wiring omission I made in Tasks 2 and 3, now three for three.
Task 4: review — spec MET, 0 Critical, 2 Important, 1 Minor. Reviewer read @clerk/backend's dist/webhooks.js to confirm verifyWebhook calls request.text() on a Request built from Fastify's raw buffer, so the original bytes genuinely reach the verifier — no re-serialization bug. It also cross-checked encapsulation empirically: running the ops/sync/webhook/cli suites together (40/40) proves the buffer parser did not leak outside the plugin, since any leak would have broken every JSON-body test instantly.
Task 4: device revocation confirmed STRUCTURAL — devices.membership_id references memberships(id) ON DELETE CASCADE (001-hub-core.sql:43), so deleting the membership cascades to devices. The test mints a real token, verifies it works, fires the webhook, then asserts verifyDeviceToken rejects — the actual security property, not a row count.
Task 4: Ruling RC8 — ACCEPT the permanent-retry behaviour (Important #1). upsertMembership throws 500 when the org/user is not yet mirrored so Clerk retries, which handles out-of-order delivery; a never-delivered dependency retries forever with an indistinguishable 500. It fails SAFE (an unmirrored membership means no device token can ever be minted for it), so this is an observability gap, not a security hole. Cost if wrong: noisy retries with no terminal signal — follow-up ticket, not a blocker.
Task 4: Ruling RC9 (resolves Important #2) — over-cap membership state is deliberately NOT persisted; the handler does a live COUNT plus a log warning, since the brief forbade a new migration. Confirming that as the design rather than changing it: entitlements are computed live from COUNT(memberships) against the entitled seat count, with only the Stripe-derived quantities cached. Carried explicitly into Task 6's dispatch so the mismatch cannot be discovered late. Cost if wrong: a per-request COUNT on membership checks — trivial at this scale, and it avoids a cached flag that can drift from reality.
Task 4: minor (deferred): no ordering guard across DIFFERENT events — a delayed stale organizationMembership.updated could overwrite a newer role change, since nothing compares event timestamps. Outside the brief's idempotency requirement (which covers replaying the SAME event, handled correctly) and low likelihood given near-real-time delivery. Surface at final review.
Task 4: complete (commits 07b3884..9c1c677, review clean)
Task 5: dispatched (implementer cloud-impl-5, sonnet, BASE 9c1c677)
Task 5: FIRST ATTEMPT FAILED — cloud-impl-5 died on a network error (ENOTFOUND), not a completion. Verified the worktree myself: HEAD still 9c1c677, no commits, no report, only an uncommitted `npm install stripe` (stripe@^22.6.0). Nothing lost, nothing half-finished. Committed the plan doc onto the branch (5a11975) and re-dispatched as cloud-impl-5b telling it exactly what state it inherited. Note for the record: an idle notification with failureReason is NOT a completion — treating it as one would have sent a reviewer at an empty diff.
Task 5: retry done, commit d190a61. 28 new tests; hub suite 153/153; full repo 342 files / 2773 tests; tsc + build clean (005 copied to dist, stripe stays external, no better-sqlite3/node-pty leak).
Task 5: OPEN QUESTION — the migration has no separate Business-seat column, so business_seat_* quantities are folded into seats_purchased alongside cloud_seat_*. Cloud and Business are DIFFERENT tiers with different formulas (Cloud = 3 included + purchased; Business = purchased only, 10-seat minimum, no included base), so conflating them may lose the tier distinction the entitlement layer needs. Asked the reviewer to rule plainly and name the minimal fix. Not dispatching Task 6 until this is settled — building the entitlement formula on a conflated field is exactly the kind of error that surfaces as wrong billing limits for real customers.
Task 5: also flagged for review — checkout/portal HTTP routes were added inline in server.ts (brief omitted them, Task 7 needs them). Asked whether the path shape fits, whether inline placement fits the plugin-per-domain convention, and critically whether they are org-scoped so a member of org A cannot open checkout or portal for org B.
Task 5: review dispatched (cloud-review-5, sonnet, 5a11975..d190a61)
Task 5: review APPROVE — spec MET on every point, 0 Critical, 2 Important. Reviewer verified no client-side price/amount/currency manipulation path (traced request body → handler → resolved server-side price id), grepped for product/price `.create|.update|.del` and found ZERO (live catalogue untouched), confirmed quantities derive from subscription items via a 4-item test, and confirmed the webhook's content-type parser is not fastify-plugin-wrapped so it cannot leak to the ops/sync plugins.
Task 5: billing routes CONFIRMED org-scoped — the unmodified onRequest hook checks the URL's :orgId against the token's resolved org and 403s BEFORE the handler runs, and the handler additionally reads request.hubOrgId rather than request.params.orgId. No cross-org path.
Task 5: Ruling RC10 (resolves the OPEN QUESTION) — the Business/Cloud folding is a REAL defect, and the reviewer found a sharper form than either the implementer or I had stated: selling `cloud_seat_monthly` with no `cloud_base` line yields `seats_included: 0, seats_purchased: N`, byte-identical to a legitimate Business org, so Task 6 could not distinguish the tiers at all. A `seats_included === 0` heuristic is not a sound basis for limits real customers experience. Fix: explicit `tier` column set from the lookup-key family. Cost if wrong: a mis-set tier gives an org the other tier's limits — visible and correctable, versus a heuristic that is wrong invisibly.
Task 5: Ruling RC11 — permit editing migration 005 IN PLACE rather than adding 006, conditional on the implementer verifying via `git branch -a --contains` and absent upstream that 005 exists only on feat/hub-cloud and has never been released. Same reasoning as Plan 1's ruling on 004: the append-only rule protects SHIPPED migrations, and this one has shipped nowhere. Required verification before editing, not assumption. Cost if wrong: an edited migration that another branch already applied would diverge — hence the check.
Task 5: Ruling RC12 — on the two ambiguous subscription shapes, required deliberate handling rather than a guess: a subscription carrying BOTH cloud_base and business_seat lines must pick a deterministic winner and log loudly (checkout does not currently prevent this); a subscription with NEITHER family must NOT fail closed, because wrongly suspending a paying org is worse for the customer than a slightly generous limit. Cost if wrong: a generous limit on a malformed row — recoverable, unlike a lockout.
Task 5: fix round 1 dispatched to cloud-impl-5b (RC10 + RC11 + RC12 + HTTP-level billing route tests)
Task 5: fix round 1 (2 addressed, 0 open — tier column with CHECK, derived from disjoint lookup-key sets independent of seat counts; 4 HTTP-level billing route tests; commits d190a61..fda8ffe). 166/166 hub, 2786 repo-wide.
Task 5: RC11 independently re-verified by the re-reviewer, which went further than the implementer: besides `branch -a --contains`, `@{upstream}` and `ls-remote`, it fetched origin/main and confirmed `git show origin/main:src/hub/migrations/005-hub-entitlements.sql` fails outright. Migration 005 has existed on no remote ref, so the in-place edit was safe and no 006 was needed.
Task 5: RC12 verified in code, not just claimed — the tier==='none' branch OMITS the entitlement columns from the INSERT/UPDATE column list entirely, so ON CONFLICT DO UPDATE cannot zero an existing row; a test shows seats_included:3, seats_purchased:4, agent_packs:2 surviving a no-recognized-key resync. A brand-new row gets schema defaults naturally. Org suspension still tracks subscription.status independent of tier, so the 'none' path cannot suspend a paying org.
Task 5: mixed-family resolution confirmed order-INDEPENDENT (two boolean flags from Set membership, not iteration order), with a dedicated test; warning payload carries only subscriptionId, no secrets. SyncLogger grepped tree-wide: two call sites, both updated, no secret in any payload.
Task 5: fix round 2 complete, commit 790b389. The implementer PROVED the new test catches its regression unprompted this time: it temporarily reintroduced the old `seatsIncluded > 0 ? 'cloud' : ...` heuristic, confirmed the new test failed first ("expected 'business' to be 'cloud'"), then reverted and verified the diff was identical to pre-probe. 42/42 billing+webhook, 167/167 hub suite.
Task 5: Ruling RC13 — accepted fix round 2 WITHOUT dispatching a scoped re-review, contrary to the usual per-round rule. Justification: I verified the diff myself as strictly test-only (`git diff --stat` shows one test file, +47 lines, zero src/ files touched, worktree clean), and the implementer had already demonstrated the observed failure — the exact evidence a re-reviewer would have been asked to produce. Cost if wrong: a test-only diff cannot alter runtime behaviour, and the full 167-test hub suite passes; worst case is a redundant or poorly-worded test, which the final whole-branch review will still see.
Task 5: complete (commits 5a11975..790b389, review clean)
Task 6: dispatched (implementer cloud-impl-6, sonnet, BASE 790b389) — unblocked now that tier is explicit.
Task 6: implementer STALLED — went idle having written src/hub/entitlements.ts, test/hub-entitlements.test.ts, edits to presence.ts/ops.ts/server.ts and the full report, but with NOTHING committed, no tests run, and no reply. Different failure from Task 5's network death: the work existed, it just was not finished. Verified the worktree myself before acting, then resumed the agent to test, commit and report rather than committing its unverified work for it. Second time in this plan that checking state before trusting an idle notification prevented reviewing an empty or unverified diff.
Task 6: complete on resume, commit c9b5264. 22 entitlement tests; all 20 hub files 189/189; full repo 343 files / 2809 tests; tsc clean; no failures at any point.
Task 6: implementer decision #1 is a real catch against MY brief — it moved the capacity check from the ops dispatch site into registerAgent, because an idempotent RE-registration of an existing agent must still succeed when the org sits exactly at cap. My literal wiring would have refused a reconnecting daemon its own agent. Flagged to review to confirm registerAgent is the ONLY path that can create an agent row, since otherwise capacity is bypassable.
Task 6: judgment calls recorded — Business gets 3 agents/seat with no pack support (so Business cannot undercut Cloud's agent-pack revenue at equal headcount); sso_enabled passes through unconditionally for both tiers (always false for Business today, works automatically if a Business SSO price is ever added); 'none' tier honours cached seats/packs when a prior healthy sync left them, else falls back to the pre-existing orgs.seat_cap (default 5) at 3 agents/seat — never zero, never locked out.
Task 6: review — 0 Critical, 2 Important, 1 Minor. Reviewer grepped every `INSERT INTO agents` repo-wide and confirmed the relocated capacity check is COMPLETE (presence.ts is the only hub path; the four other hits belong to the unrelated local single-user agents table). Also confirmed assertOrgWritable is called exactly once and on no GET, that all 7 ops mutate, and that GET /entitlements resolves org from the verified token rather than the URL param.
Task 6: Ruling RC14 — fix the TOCTOU race. Check and INSERT share a transaction but production is READ COMMITTED with no row lock, so two concurrent registrations of distinct new names can both read "under cap" and both commit, leaving an org over the capacity we sell. Directed `SELECT id FROM orgs WHERE id = $1 FOR UPDATE` as the first statement in registerAgent's transaction — portable, no extension, locks only that org's row. Crucially the reviewer noted PGlite is single-connection and CANNOT expose this, so the 22/22 pass was never evidence; I required the implementer to state honestly what it can and cannot prove rather than fake a concurrency test. Cost if wrong: a per-org lock serialises registrations within one org only — negligible, since registration is rare relative to heartbeats.
Task 6: Ruling RC15 — the seat cap was computed but enforced NOWHERE (Task 4's webhook explicitly deferred it here, and it was a silent gap rather than a stated decision). Decided: do NOT retroactively disable a member Clerk already admitted — a teammate who appears in the org but silently cannot work, with no remedy they can act on, is worse product than an over-cap org. Enforce instead at DEVICE-TOKEN MINTING: the first N members by membership created_at (deterministic and explainable) may connect daemons; beyond that, a clear refusal naming the limit. Everyone can still sign in and view. Rationale: a seat's real cost to us is a connected daemon, so that is the right thing to meter, and view-only access strands nobody. Over-cap status also surfaces in GET /entitlements so the UI prompts an upgrade instead of leaving people to discover it at a failure. Cost if wrong: an org could exceed member count without paying while still being unable to run extra daemons — revenue exposure limited to the thing that does not cost us compute.
Task 6: minor folded into the same round — the capacity error unconditionally suggests buying an agent pack, which has no Business SKU, so a Business admin at cap is told to buy something that does not exist for their tier. Made tier-aware.
Task 6: fix round 1 dispatched to cloud-impl-6 (RC14 + RC15 + tier-aware message)
Task 6: implementer STALLED A SECOND TIME — again went idle with all fix work uncommitted and unverified, including edits to devices.ts and server.ts which carry reviewed auth code. Checked the worktree and resumed rather than committing unverified work for it. Then delivered: commit 37c491e, 32 entitlement tests, 199/199 hub, and a follow-up confirming the full repo at 343 files / 2819 tests.
Task 6: the implementer's honesty on the concurrency proof is the model outcome for RC14 — it stated plainly it could NOT prove two-transaction concurrency under single-connection PGlite, wrote no test claiming otherwise, and labelled its lock test as proof-of-wiring in both a code comment and the report. It also spotted unprompted that mintDeviceToken carries the same race class and locked it too.
Task 6: fix round 1 (3 addressed, 0 open — FOR UPDATE first statement in both registerAgent and mintDeviceToken; assertSeatAvailable ranked created_at ASC, id ASC; tier-aware capacityRemedy; commits c9b5264..37c491e). Re-reviewer confirmed lock ordering by direct read, verified the test-honesty claim, and confirmed verifyDeviceToken, the onRequest hook and assertOrgWritable are untouched.
Task 6: complete (commits 790b389..37c491e, review clean)
Task 6: SCOPE GAP SURFACED BY THE RE-REVIEW — `mintDeviceToken` has ZERO HTTP routes wired to it; it is reachable only from tests. So no daemon can currently obtain a token, and the whole point of the product (several people's daemons on one board) is unreachable end to end. Root cause is mine: PLAN 2 (the daemon sync client — `orchestra org join`, the sync loop, the offline queue) was scoped in the spec but NEVER WRITTEN, and I went straight from Plan 1 to Plan 3. Task 8's smoke test ("join a daemon, see a card from another machine") cannot pass without it.
Task 6: Ruling RC16 — add a minimal Clerk-authenticated device-mint route to Task 7 (`POST /api/v1/hub/orgs/:orgId/devices`, returning the plaintext token exactly once) so a signed-in member can mint a token in the browser and paste it into their daemon. This unblocks the end-to-end demo without building Plan 2. Full `orchestra org join` automation, the SSE sync loop and the offline queue remain Plan 2 and must be surfaced to the operator as genuinely-remaining work, not quietly folded in. Cost if wrong: a manual paste step in the first demo — acceptable versus stalling all of Task 7/8 behind an unwritten plan.
Task 7: dispatched (implementer cloud-impl-7, sonnet, BASE 37c491e)
Task 7: implementer done, commits e3e57fc (cherry-picked Clerk UI from the unmerged feat/clerk-web) + 0a829d3. web 12/12, root 344 files / 2822 tests, both tsc clean, local-mode AND hub-mode builds both succeed.
Task 7: four disclosures — (a) org board is read-only, deliberate; (b) two routes beyond the brief: GET /api/v1/hub/me (the browser had no way to learn its own internal org id) and the POST devices mint route I asked for in RC16; (c) fixed a hubApi.ts bug that surfaced raw {"error":...} JSON as the UI message instead of the text, which the seat-cap message requirement depends on; (d) no boards-listing route exists, so the board is one flat org-wide view rather than per-project.
Task 7: review APPROVE — spec MET on all six steps, 0 Critical, 0 Important, 1 Minor. Reviewer verified every "must not change" file is byte-identical, diffed the cherry-picked Clerk commit against its source patch (identical bar the lockfile merge), and built BOTH configured and unconfigured to confirm local mode is untouched — ClerkProvider is never even mounted without the key, so there is no Clerk runtime cost in local mode, not merely a hidden UI.
Task 7: security checks clean — GET /me returns only {user_id, org_id} from the verified token with no enumeration surface; the mint route is org-scoped by the same untouched onRequest hook (a device token hitting it gets hubUserId === null and an explicit 403); seat enforcement stays singular inside mintDeviceToken's FOR UPDATE transaction rather than being duplicated in the route; the plaintext token lives only in component state, rendered React-escaped, copy-to-clipboard, never in localStorage/URL/console; and the hubFetch error fix cannot leak internals because every unmapped 5xx already collapses to a fixed body server-side.
Task 7: Ruling RC17 — the Minor is a MONEY bug, not cosmetic: BillingPage's upgrade button always starts checkout for `cloud_base_monthly` regardless of tier, so a Business org clicking it would open a Cloud subscription. Latent today (no UI path reaches Business), but the Stripe account is live and can take real money, and a customer subscribed to the wrong tier is a support-and-refund mess rather than a display glitch. Folding the fix into Task 8's dispatch rather than spending a separate round — Task 8 is the last implementation task and the final whole-branch review still sees it. Cost if wrong: a few lines of UI change land in a deploy-focused commit, which is untidy but visible.
Task 7: complete (commits 37c491e..0a829d3, review clean)
Task 8: dispatched (implementer cloud-impl-8, sonnet, BASE 0a829d3)
Task 8: implementer done, commit b3a99c0. hub 202/202, web 12/12, tsc clean both packages, real builds succeed both.
Task 8: three decisions ACCEPTED as-is — (a) vercel.json at repo root using `--prefix web` rather than a dashboard-only Root Directory setting, since tracked config beats an untracked dashboard toggle; (b) the CI web-test step left UNWRAPPED by exact-commit-evidence, because wrapping it would require adding to required_gates, changing contract_sha256 and risking invalidation of retained release evidence — matches the precedent the existing provider-catalog step already documents; (c) the smoke test written as an unexecuted 9-step checklist rather than a fabricated run, correctly flagging that no `orchestra hub join` CLI exists so daemon steps are curl-simulated.
Task 8: Ruling RC18 — the implementer FOUND but only DOCUMENTED a real defect: billing.ts hardcodes checkout/portal redirect targets to app.orchestraboard.dev and never uses WEB_ORIGIN. Directed a fix instead. Reasoning: the subscription still activates via webhook, so we TAKE THE MONEY and then dead-end the browser on an unconfigured domain — the customer has just paid and cannot tell whether it worked. Documenting that ships a known-bad payment flow. The operator's domain is also orchestraboard.COM, so the hardcoded value is likely wrong on its face, and WEB_ORIGIN is already threaded through hubEnv() for CORS, so the correct value is in hand. Required a decision on the unset case with a stated preference for refusing to create the session naming the missing variable, rather than guessing a domain — a loud failure at deploy time is far cheaper than a silent one at checkout. Cost if wrong: a misconfigured WEB_ORIGIN blocks checkout entirely instead of redirecting badly, which is visible immediately and fixable in one env var.
Task 8: fix dispatched to cloud-impl-8 (RC18 + hosting.md update). First send crossed with the implementer going idle; I verified the tree was clean and nothing lost before re-sending, then the re-send crossed with its reply — the fix was already committed at 7c89c58. No duplicate work.
Task 8: fix complete, commit 7c89c58. 207/207 hub across 21 files (5 new billing tests), web 12/12, tsc + real builds clean both packages.
Task 8: review APPROVE — 0 Critical, 1 Important. Reviewer VERIFIED the justifications rather than accepting them: read scripts/exact-commit-contract.mjs to confirm contractSha256 hashes required_gates so the unwrapped-CI-step reasoning is accurate; grepped src/ and web/src/ to confirm no hardcoded orchestraboard.dev survives; confirmed requireWebOrigin runs BEFORE any Stripe call in both session creators so no charge path opens with a broken return; confirmed the real error is logged server-side only and collapses to the generic body; and checked the "incidental doc anchors" against b3a99c0 to confirm they were genuinely broken rather than disguised scope.
Task 8: Important (carried to the final review's triage, not a separate round) — the new CI web-test step sits early without continue-on-error, so a web-test failure skips ~16 later steps and leaves them "missing" rather than individually diagnosed. The job still fails correctly, so this is diagnostics, not correctness. Cheapest fix is moving the step later (the provider-catalog precedent) rather than adding a required_gates entry, which would change contract_sha256.
Task 8: complete (commits 0a829d3..7c89c58, review clean)
FIX WAVE COMPLETE, commit 04acd90 — all 4 Criticals + 4 Importants. Full root suite 345/345 files, parallel and serial.
FINAL RE-REVIEW (OPUS): all 8 ADDRESSED, 0 new Critical, 1 new Important, MERGE-READY. It re-ran every suite itself and verified each claim rather than trusting the report. Notably it DISPROVED part of the fixer's own worry: the default-board fragility is milder than described, because a missed organization.created leaves no orgs row at all, so the user is 403'd at sign-in rather than stranded board-less. It also proved the concurrent-replay case the fixer said it could not: the ON CONFLICT upsert locks the org row, and under READ COMMITTED the second delivery's next statement sees the committed project, so it converges by construction.
FINAL RE-REVIEW confirmed the two riskiest questions: seeding a subscription into hubFixture did NOT weaken the ~20 dependent test files (every capacity/seat/tier test re-seeds the exact numbers it asserts; cross-org tests still 403/404 for their original reason), and a never-subscribed org CAN still buy its way out — assertOrgWritable guards exactly two call sites, leaving checkout, portal, entitlements, boards, reads and token minting outside it. No deadlock.
Ruling RC19 — took the new Important rather than parking it, despite the reviewer saying it need not hold the merge. refuseSecondSubscription reused the SUSPENSION status set to answer a different question: "would a second checkout collide with an existing subscription?" is false only for canceled and incomplete_expired, whereas unpaid and paused subscriptions still exist and can resume. For those two, the server guard let a second subscription through and re-opened the exact C2 double-billing harm. The UI shields it, but I had told the fixer the SERVER guard was the one that had to be real, so shipping a known API-level bypass of it was not acceptable. Cost if wrong: a one-line set, fully tested.
Ruling RC20 — the implementer flagged that my fix instruction CONTRADICTED ITSELF: I stated the principle as "a subscription still exists that could resume", then asked for `incomplete` to be unblocked, which violates it. It implemented the safe reading and surfaced the conflict instead of silently picking. Confirmed its reading: `incomplete` must block, because the customer can still complete the SCA challenge and a second checkout would then yield two live subscriptions on one card. Blocking ~23h until Stripe expires it beats double-charging. Cost if wrong: a customer who abandoned an SCA challenge waits for expiry — visible, bounded, and reversible.
Ruling RC21 — PARKED and surfaced to the operator rather than fixed: the fix wave renamed SUSPENDING_STATUSES to SUSPENDING_SUBSCRIPTION_STATUS, which EVADES pattern index 1 of check-beta-quality-matrix.mjs rather than satisfying it. The constant genuinely is a status→state mapping, so the classification review was routed around by naming. The gate self-labels as advisory-regex so severity is low, but a gate dodged by rename is exactly the kind of thing that should reach the operator rather than sit in a ledger. Cost if wrong: an unclassified state machine escapes an advisory inventory.
FINAL STATE VERIFIED BY ME: HEAD 94082b7, worktree clean, 18 commits on plan 3, full repo suite 345 files / 2882 tests passing.
FINAL REVIEW: DO NOT MERGE — 4 Critical, 5 Important, 2 Minor. The single most valuable review of either plan. It traced a real paying customer end to end rather than checking components, which is exactly what eight scoped reviews structurally could not do.
FINAL C1 (blocks) — NO WAY TO CREATE A BOARD; the hosted product is unusable. Nothing under src/hub/ ever inserts into projects or boards, yet every write op requires an existing board_id. A customer pays $20 and lands on an empty read-only board with nothing to point a daemon at. Invisible to all eight scoped reviews because EVERY hub test seeds boards with raw SQL (test/support/hub-sql.ts) — the tests passed while the product had no entry point. docs/hosting.md:299 even says "<boardId> must already exist", which is unsatisfiable. Root cause is mine: Plan 1's ops layer assumed boards would arrive via Plan 2's daemon sync loop, I never wrote Plan 2, and no Plan 3 task owned the gap.
FINAL C2 (blocks) — the billing page's PRIMARY button double-bills an existing Cloud customer and destroys entitlements they paid for. It routes tier 'cloud' into checkout, creating a second subscription on the same customer; since subscriptions.org_id is a PRIMARY KEY the sync overwrites the row and the first subscription's seats/packs reset to 0. Cancelling either then suspends the org while the other is still charging. Zero coverage: the web suite renders no component, and no webhook test puts two subscription ids against one org.
FINAL C3 (blocks) — a daemon DEVICE TOKEN can cancel the org's subscription. The auth hook accepts device tokens on every /api/v1/hub/ path and the billing routes check only org scope. That token is long-lived, never expires, and the runbook has people paste it into a laptop config; its holder can cancel the plan, change the payment method, and read invoices with the billing address. test/hub-billing.test.ts ASSERTS this succeeds — the suite was blessing the vulnerability. Compounding: memberships.role is mirrored then never read, and revokeDevice has no HTTP route, so a leaked token cannot be revoked through the product.
FINAL C4 (blocks) — NOT PAYING IS STRICTLY BETTER THAN PAYING. The no-subscription fallback uses orgs.seat_cap (default 5) for 5 seats and 15 agents; Cloud base at $20/mo gives 3 seats and 9 agents, and nothing requires a subscription to exist at all.
FINAL I2 (blocks, reviewer's judgement and mine) — only 'active'/'trialing' count as active, so a single declined card suspends a paying customer on the FIRST failed charge while Stripe smart-retries for two to three weeks. Converts a routine decline into a customer-visible outage.
FINAL: reviewer CONFIRMED the auth story otherwise holds (org scoping via mirror-table joins, no cross-org path, both webhooks fail closed on raw bytes) and that the free local product is genuinely untouched (empty diff over src/server.ts, src/agent-os/**, api.ts, deviceAuth.ts; both UI entry points fail closed on unset env).
FINAL: triage of my known-issues list — Plan 2's absence reclassified from "missing convenience command" to BLOCKING, because it is the direct cause of C1. CI ordering, deps.env, structural idempotency and the unprovable FOR UPDATE all confirmed as follow-ups.
Fix wave dispatched (cloud-fix-final, OPUS, one dispatch): C1 (create + auto-create + list boards), C2 (server-side guard AND client routing), C3 (require Clerk principal on billing, role-gate the portal, add a revoke route, fix the test that blessed it), C4 (never-subscribed cannot write, distinguished from the ruled 'none'-tier case), I2, I3 (SPA rewrite + acknowledge checkout), I4 (runbook corrections), CI ordering. Explicitly NOT in scope: selling the nine unsellable prices, and Plan 2's sync loop.
ALL 8 CLOUD TASKS COMPLETE. Final whole-branch review dispatched (cloud-final-review, OPUS, 5b91da1..7c89c58, 16 commits) with the full known-issues list for triage — CI ordering, read-only board, the unwritten Plan 2, the deps.env test-seam split, structural-only webhook idempotency, and concurrency guarantees unprovable in a single-connection harness.
Task 7: review dispatched (cloud-review-7, sonnet, 37c491e..0a829d3) — focus: exactly what GET /me discloses (new information surface; every field must be the caller's OWN), whether the mint route stays org-scoped and routes through mintDeviceToken so assertSeatAvailable and the FOR UPDATE lock still apply, whether the plaintext token is ever stored in localStorage/URL/console rather than shown once, and whether the error-surfacing fix now leaks server internals into the UI.
Task 7: NOTE for the operator summary — combined with (a) and the absent Plan 2, the hosted UI is currently VIEW-ONLY: no card creation from the browser, and no daemon sync loop. The hub is writable via its API and by daemons holding a token, but the browser cannot create work. State this plainly rather than letting "Task 7 complete" imply a fully interactive board.
Task 6: review dispatched (cloud-review-6, sonnet, 790b389..c9b5264) — focus: whether the relocated capacity check is COMPLETE (enumerate every agent-creation path), off-by-one in both directions, both halves of suspended-org behaviour, live COUNT rather than a cached member count, no Stripe call on the request path, and org-scoping of the new GET /entitlements route.
Task 5: fix round 2 dispatched to cloud-impl-5b — one Minor: no test isolates the exact discriminating pair (cloud_seat with NO base vs business-only) that motivated the tier column. Code verified correct by reading, but that pair is precisely the regression guard: a future refactor reintroducing a seat-count heuristic would leave every current test green. Same failure shape as this plan's build-assertion test, which looked correct and could not fail.
Task 4: review dispatched (cloud-review-4, sonnet, 07b3884..9c1c677) — focus: whether the raw body reaching the verifier is genuinely the original bytes (a re-serialized body passes every test and fails in production on key-order/whitespace differences), whether membership.deleted REALLY revokes device tokens (assert a later verification fails, not just that a row vanished), and whether structural idempotency holds for each of the nine handlers under at-least-once delivery.
Task 3: review dispatched (cloud-review-3, sonnet, 9884a80..942f5d4) — focus: device-token regression, real revocation via mirror tables (not JWT claims), the auth-failure oracle staying closed across the new Clerk failure paths, and fail-closed behaviour when clerkSecretKey is unset.
Task 2: review dispatched (cloud-review-2, sonnet, 98c37cb..9884a80) — asked it to check for substring-vs-exact origin matching, whether CORS actually activates in the deployed entrypoint, and whether VITE_HUB_BASE_URL is validated before a Clerk token is sent to it.
Task 1: scoped re-review dispatched (cloud-rereview-1, sonnet, 84bfc5b..98c37cb) — told it to reproduce the regression itself rather than trust the report, and to reason explicitly about which import forms the regex does and does not match.
Task 1: review dispatched (cloud-review-1, sonnet, 5b91da1..84bfc5b) — asked it to verify the native-dep exclusion holds across EMITTED CHUNKS (not just the entry file, since the dynamic-import workaround could reintroduce a shared chunk) and to confirm the new build-assertion test would actually fail on a regression.
